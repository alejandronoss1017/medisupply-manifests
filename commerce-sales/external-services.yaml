apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: sales-db
spec:
  hosts:
    - dynamodb.us-east-2.amazonaws.com
  ports:
    - number: 443
      name: https
      protocol: TLS
  resolution: DNS
  location: MESH_EXTERNAL
  exportTo:
    - "*"


